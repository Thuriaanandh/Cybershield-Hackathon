# LiveBoot Sentinel - Server Requirements
# Python >= 3.11 required

# FastAPI framework
fastapi==0.115.5
uvicorn[standard]==0.32.0

# SQLAlchemy ORM with async support
sqlalchemy==2.0.36
aiosqlite==0.20.0           # SQLite async driver

# Optional PostgreSQL support
# asyncpg==0.30.0

# Pydantic v2 (included with FastAPI)
pydantic==2.9.2

# Rate limiting (using in-memory implementation — no redis dep)
# slowapi==0.1.9  # alternative if Redis-backed limiting needed

# HTTPS / TLS
# uvicorn uses stdlib ssl — no extra dep needed for basic TLS

# Structured logging
python-json-logger==2.0.7
